# Stage 0 baseline

The ignored portable bundle was inspected before implementation.  This folder
contains only a manifest and source snapshots of the classroom-owned Python
and PowerShell files.  It must never contain `data/`, `logs/`, passwords,
tokens, database files, or the portable Python runtime.
