# 教师使用指南

当前对应便携包版本：**2.1.8** 测试候选。

已有 2.1.8 安装可应用 [审计修复补丁](PATCH_2.1.8_README.md)。请先停止并联合备份，再覆盖程序文件；首次启动自动升级课堂数据库至 v5。

## 全新安装

Windows 系统前置条件：安装最新的 [Microsoft Visual C++ x64 运行库](https://aka.ms/vc14/vc_redist.x64.exe)。包内 PyTorch 仍依赖系统 C++ 运行库（包括 `MSVCP140_ATOMIC_WAIT.dll`），此前在开发电脑通过验证不代表新电脑已具备这些依赖。若出现 `torch_python.dll` / `WinError 126`，先安装或修复该运行库，按安装程序提示重启，再重新初始化。只需在运行教师端服务的电脑处理，无需重装 Python，也不要删除现有 `data`。

1. 将完整 ZIP（如 `openwebui-classroom-2.1.8-windows-x64.zip`）解压到可写目录，支持中文和空格路径。不要放在需管理员授权才能写入的系统目录。
2. 双击「初始化新课堂.cmd」。它用包内准确版本生成并验证新数据库；已有 `data` 时拒绝覆盖。
3. 双击「启动课堂.cmd」。首次输入提供商 HTTPS 基础地址（通常以 `/v1` 结尾）、实际模型 ID 和 API key。地址和模型名会保存到 `data/classroom/provider-config.json`，API Key 使用 Windows 当前用户加密后保存，后续启动自动读取。教师管理员邮箱会记住；已有安装只有一个管理员时会自动读取该邮箱。管理员密码首次输入并成功启动后，使用 Windows 当前用户加密保存到 `data/classroom/teacher-login.json`，以后启动自动读取；网页端仍正常登录。更换电脑后需要重新填写密码。在网页修改管理员密码后，运行 `Reset-SavedTeacherLogin.cmd` 清除旧的启动凭据，下次启动输入新密码即可。此操作不会修改网页账户或密码。
4. 启动脚本在本机验证 Pipe 内容和原生安全配置后开放学生功能。内部课堂服务只监听 `127.0.0.1:8790`；浏览器访问教师机 `3000` 端口。默认 WebUI 只绑本机；学生要从局域网访问时，用「启动课堂.cmd」的局域网方式或 `Start.ps1 -Lan`（明文 HTTP，无 TLS）。
5. 教师登录原生 WebUI，再打开 `/classroom/teacher/`。管理页左侧分为「审核队列 / 学生与额度 / 使用概况 / 名册导入 / 课堂设置」五个分区，顶部常驻服务状态、刷新和「暂停全班 AI」。在「名册导入」中导入名册：可用 CSV（表头必须为 `Name,Email,Password,Role`，Role 只能为 `user`，一次最多 30 人），或在「单独添加一名学生」中直接填写姓名、账号和初始密码。预览不保存明文密码，过期后需重新预览。
6. 学生首次登录会进入改密页面。改密成功必须重新登录；教师重置也撤销旧会话。学生回答在生成阶段会按服务端事件增量显示；提问框有「当前字数/1000」提示，接近上限时变红；额度显示为「今日可用 · 已用 · 处理中」。
7. 「临时免密 AI 链接」：在「课堂设置」分区开启开关并复制链接（需 `-Lan` 启动后学生才能用教师机 IP 访问）。访客页沿用学生聊天界面样式。打开链接无需登录、不占学生次数、不经审批，仍使用课堂系统提示词与课堂模型，单条提问同样不超过 1000 字。关闭开关或重新生成后旧链接立即失效。持链者可无限调用教师机 API 额度，仅建议机房临时活动使用。
8. 「审核模式」：默认教师审核；可改为 AI 审核（使用课堂同一提供商与独立审计提示词，全班共用一个审计会话；短暂网络/DNS 失败会自动重试，仍失败则回退到待教师审核，并尽量保留失败原因）或不审核（提交后直接生成）。可在设置中查看 AI 审计历史、清空审计会话上下文、编辑/恢复默认审计提示词。
9. 「使用概况」：查看今日提问概览、关键词词云、学生提问排行与时段分布。词云只统计已通过审核的提问；待审、拒绝及停课后残留的待审记录不进入。

导入出现部分失败时，重新上传同一份 CSV，可恢复之前的批次；已完成的行不会重复创建。修改模型配置时，双击「修改模型配置.cmd」，填写新地址、模型和密钥，重启课堂后生效。地址或凭据变化会让旧的排队批准回到待审核。

复制到机房或更换 Windows 登录用户后，如果保存的密钥无法解密，会提示重新填写 API Key；地址和模型名仍可复用。此时无需重新初始化课堂。不要手工把明文密钥写进 JSON。环境变量仍可覆盖保存的配置；如果指定了不同的服务地址，启动程序不会将旧密钥自动发送到新地址。

老师使用原生「课堂 AI」对话时，调用启动时配置的模型，不占学生次数，也不需要审核自己的问题。学生对话仍按当前审核模式处理。

## 保留已有账号、聊天和审批

不要覆盖原安装。使用包内 Python 执行：

```powershell
.\runtime\python\python.exe -B .\app\prepare_installation.py --native-root "原安装\data\openwebui" --legacy-review "原安装\data\review\review.db" --destination "新的安装数据目录"
.\scripts\Start.ps1 -DataRoot "新的安装数据目录\classroom" -NativeRoot "新的安装数据目录\openwebui"
```

迁移只读源库，校验准确原生 schema，把完整账号/认证表、聊天和上传目录复制到新目录，同时归档旧消息和审批。原生结构不匹配时停止，不自动猜测 Alembic revision。旧审批不会转成新扣次；原始状态保留在 legacy 归档中。原生和课堂旧库的只读快照也保存在新目录 `classroom/legacy`。

先在新目录演练并检查 `migration-report.json`，再使用新目录启动。源目录一直保留，便于受保护回滚。不要手工设置旧的 `CLASSROOM_OPENWEBUI_MIGRATION_BASELINE=verified`；新启动器检查数据库结构与实测报告。

## 审核与额度

「审核队列」支持单条和批量批准/拒绝、查看真实附件、修改后批准、教师停止；左侧导航会显示待审核数量。停留在审核页时会轻量刷新队列（无变化不整页重画）；点批准/拒绝只更新队列，不强制重拉全班学生名单。AI 失败回退的条目可标「AI回退」。审核模式（课堂设置）可为教师审核 / AI 审核 / 不审核。「学生与额度」显示学生总数、剩余次数合计等概览，并支持暂停单个学生、重置密码、名册改名、跨页勾选后统一增减额度和导出。详情弹窗打开时仍会刷新学生额度。「使用概况」提供今日词云与提问统计。操作结果统一显示在顶部提示条。拒绝、正常完成各扣一次；上游异常中断不扣次；学生主动停止依据浏览器已交付回答判断。教师停止不扣次。

额度修改先选学生，输入非零整数并查看预览；服务同时核对日期、版本和幂等键。自动刷新保留已选学生。网络错误后重试使用原操作键，避免重复调整。学生页额度显示「可用 · 已用 · 处理中」；提交时预留次数，完成后可用次数不变、已用增加。

学生可以在原生对话中使用课堂 Pipe，也可从 `/classroom/student/` 提交、停止、恢复和查历史。学生页在生成阶段按服务端事件增量显示回答；AI 审核拒绝时提示「AI助教未批准这次提问」。原生页面中的课堂状态框显示持久化回答，并向服务确认浏览器交付；服务写入输出日志不等于用户已经收到。

附件最多五个，合计 10 MiB；单张图片最多 5 MiB、两千万像素；文字文件最多 1 MiB。支持 PNG/JPEG/WebP（不接受动画）及 `.py/.txt/.md/.csv/.json/.yaml/.yml/.log`。原生上传强制关闭提取/向量化，课堂服务校验并保存真实字节。GB18030 文字请通过学生状态页选择编码。

未提交附件最多临时保留 24 小时，此后需重新上传；已经提交的附件永久归档。学生页刷新后恢复最近请求，网络重试保留原提交操作键。提问正文前后端均限制 1000 字。

教师页「课堂设置」中的「课堂系统提示词」可以修改学生新对话的统一要求，点击保存立即生效，不需要重启。已有对话及已提交审核的内容保留原提示词；需要使用新提示词时，让学生开启新对话。修改不会更换模型或 API Key。审计提示词与课堂系统提示词相互独立，仅用于 AI 审核模式。

## 备份、恢复与回滚

审计补丁说明：AI 审核读取完整文本附件；含图片或审核上下文超过 40,000 字符时转交教师查看，不自动放行。访客链接面板会建议教师机局域网地址；多网卡时请选与学生同网的地址，不能分享 `localhost` / `127.0.0.1`。教师对话附件可正常上传，已提交的附件不按临时上传清理。

先用「停止课堂.cmd」排空执行再停服务。执行：

```powershell
.\scripts\Backup.ps1 -DataRoot "数据\classroom" -NativeRoot "数据\openwebui" -Output "备份\课堂完整备份.zip"
.\scripts\Restore.ps1 -Backup "备份\课堂完整备份.zip" -TargetRoot "新的恢复目录"
.\scripts\Start.ps1 -DataRoot "新的恢复目录" -NativeRoot "新的恢复目录\openwebui"
```

备份包含课堂库、原生账号聊天库、上传文件、legacy 快照和必要安装密钥，应作为私密文件保存。服务运行时拒绝联合备份；恢复先校验全部文件哈希和 SQLite 完整性，只能恢复到新目录或空目录，失败不会覆盖原始数据。

需要回滚时，先保留新系统的完整备份（包含切换后新增记录），再恢复切换前备份到另一新目录。不要在当前数据目录上覆盖旧库，也不要删除新库以“回滚”。

可执行 `scripts/Rollback.ps1 -BeforeBackup "切换前.zip" -DataRoot "数据\classroom" -NativeRoot "数据\openwebui" -PreservedBackup "新的切换后备份.zip" -TargetRoot "新的回滚目录"` 自动按此顺序操作。回滚不自动启动，先核对恢复结果。恢复后的原生附件路径指向新目录。

## 更新程序文件（小补丁或源码覆盖）

1. 先运行「停止课堂.cmd」。
2. 将新版本中的 `app`、`web`、`scripts`（按补丁说明）合并覆盖到安装根目录，选择替换文件。
3. 若存在 `app\classroom_service\__pycache__`，可整夹删除（启动会重建）。
4. 运行「启动课堂.cmd」；教师/学生页建议 Ctrl+F5。
5. 不要删除 `data`；补丁或源码覆盖不含模型密钥与课堂数据。

## 自定义端口与故障

`Start.ps1` 接受 `-DataRoot`、`-NativeRoot`、`-WebPort`、`-ServicePort`。默认只监听本机 `127.0.0.1`；学生要从局域网访问时显式加 `-Lan`（明文 HTTP，无 TLS）。对应 `Stop.ps1 -DataRoot` 读取同目录进程记录并核对 PID、启动时间和解释器路径。服务日志在课堂数据目录 `logs`。

遇到 schema 不符、额度账目不符、时钟明显回退或未知模型配置，服务停止派发。修复系统时间或从经过验证的备份恢复后再启动；不要清空账本或手工修改预留次数。

本机防火墙仅需开放 WebUI 端口给教学局域网；不要向学生开放内部服务端口。
