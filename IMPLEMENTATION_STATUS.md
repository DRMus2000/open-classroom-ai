# 整改实施状态

版本：2.1.8 测试候选（含文档对齐现状，以及 2.1.7 起的访客页、流式回答、AI 审核加固与教师队列轻量刷新）。原审计结论描述修复前状态，未被覆盖或篡改；公开源码见 https://github.com/DRMus2000/open-classroom-ai 。

2.1.8 审计修复补丁 `2.1.8-audit1`：修复思考过程示例覆盖最终审核结论、附件未经内容审核、慢审核阻塞派发、额度版本冲突无法重新预览、学生完成状态在断网后丢失、系统提示词变更破坏幂等重试、教师无法上传附件、访客链接使用回环地址。课堂数据库新增 v5 迁移，附件所有者绑定已注册身份，保留旧附件及原生文件映射。安装与回滚见 `PATCH_2.1.8_README.md`。

补丁验证（2026-09-19）：110 项 Python 测试、4 个前端交互探针通过；准确 Open WebUI 0.11.2 的真实路由登录、首次改密、学生附件与审批、教师附件上传与对话、重置密码及旧会话撤销通过。新增测试覆盖 v4→v5 保留数据迁移、补丁前请求幂等兼容、审核总超时与停止取消、慢审核下继续派发、文本附件全文审核、图片/长附件转人工、额度冲突恢复、断网与重启后最终状态恢复、局域网分享地址、补丁文件清单与哈希。提供商仍使用隔离模拟，不代表真实模型与校园网现场验收。

2.1.8 相对 2.1.7：现行文档（README、教师指南、需求、实施状态、文档索引）对齐当前行为与公开仓库；历史审计材料标注为冻结证据；完整发行包版本号推进到 2.1.8。现场仍须安装 Visual C++ x64 运行库。

2.1.7 相对 2.1.6：访客临时链接改用学生聊天界面；提问框显示当前/1000 并在接近上限时变红；AI 未批准时提示「AI助教未批准这次提问」；生成阶段按 events 增量拉取，避免整包重绘造成的卡顿；词云只统计已通过审核的提问，停课重启后残留的待审记录不进入。补充：AI 审核改为非流式完整读取并可解析夹杂思维过程的 JSON；DNS/连接类失败最多重试 3 次；失败原因尽量保留上游细节；教师审核队列在审核页轻量刷新、批准/拒绝不再整页重拉学生名单。

2.1.6 相对 2.1.5：修复学生提问/改密页样式损坏与桌面端误显示的侧栏关闭按钮；AI 审计历史改为定高滚动、单行截断；未审核通过的提问不进入词云；学生提问前后端限制 1000 字。

2.1.5 相对 2.1.4：学生生成阶段按 durable events 增量渲染；额度文案显示可用/已用/处理中，活动请求期间也会刷新 `/me`；教师详情弹窗不再阻止学生额度刷新；审核模式 teacher/ai/none（AI 失败回退教师）；AI 审计提示词与历史；使用概况词云与今日统计。

2.1.4 相对 2.1.3：教师页侧栏分区；CSV 外单独添加学生；临时免密 AI 链接（需 `-Lan`）；访客并发默认 64。

2.1.3 相对 2.1.2：学生 WebSocket 默认拒绝、教师原生模型出口锁定、Start.ps1 默认仅本机监听（局域网需 `-Lan`）、Cookie 写请求 Origin 校验、教师上传课堂校验、公开 `/health` 仅返回 ready。

已实现：单服务签名桥接与运行调度、默认拒绝学生旁路、成功登录登记与撤销、原生密码/文件 hook、异步 Pipe、浏览器交付确认、停止取消、重启恢复、日界清理、额度版本与幂等保护、真实图片校验、服务端历史、教师和学生操作页面、联合备份安全恢复、准确原生结构校验及只读源库迁移、完整运行时白名单打包。

已验证：便携 Python 中 56 项回归通过。准确 Open WebUI 0.11.2 的真实登录、首次改密、文件上传、学生旁路拒绝、聊天审批到回答落库及教师重置密码链路通过；提供商使用隔离模拟。中文空格路径中的全新数据库完整启动、自动安装 Pipe、状态查询、重复启动和排空停止通过。30 名模拟学生各一次唯一派发，峰值并发 4，全部 used=1/reserved=0。现有数据迁移演练保存 2 个账号及 2 条认证记录、4 个聊天、18 条消息、6 条旧审批，记录内容摘要可复核，旧记录扣次为 0，未修改源库。三份前端脚本通过语法检查。

新增恢复保护：提供商地址/凭据配置摘要持久化并使旧批准失效；1501 段回答跨分页完整转发；账号创建响应丢失后同 CSV 恢复；24 小时未提交附件清理；联合恢复重写原生附件路径；导出下载通过流式桥接；学生页保留失败提交的操作键。源基线的 8 份自有文件已从原始 ZIP 提取并逐一匹配原 manifest 哈希。

尚不能作为现场验收通过的证据：真实提供商调用、30 个真实学生浏览器的校园网延迟/断网测试、全新 Windows 主机实际解压使用。当前测试中的提供商均为隔离模拟，不产生费用。最终验收应依原计划 K/L 章节执行，不以单元测试数量代替现场验收。

浏览器补充复验：合成教师登录、管理页加载、暂停和恢复全班 AI 均通过。首次解压验收发现桥接客户端逐请求初始化导致约 2.7 秒延迟；2.1.1 改为复用连接并在原生生命周期退出时关闭，同时禁用不使用的原生 OpenAI/Ollama/arena 模型发现。
