# 课堂 AI 2.1.8 审计修复补丁

补丁标识：`2.1.8-audit1`。适用于已安装的 **2.1.8 完整便携包 / Open WebUI 0.11.2**。

本包包含 `app`、`web`、`scripts` 和更新后的文档，不包含 Python 运行时、模型密钥、账号或课堂数据，不能单独启动。

## 安装

1. 在原安装目录运行「停止课堂.cmd」，确认服务已停止。
2. 按教师指南执行联合备份，保存补丁前的数据以及旧程序文件。
3. 解压补丁，将其中 `openwebui-classroom-windows-x64` 文件夹内的内容合并复制到原安装根目录（即包含「启动课堂.cmd」和 `runtime` 的目录），替换同名文件。不要额外嵌套一层目录。
4. 启动课堂。第一次启动会自动将课堂数据库从 v4 升级到 v5，保留学生、额度、审批和已有附件映射；原生 WebUI 数据库结构不变。若日志出现 `SCHEMA_V5_PREFLIGHT`，数据库仍停留在 v4，先联合备份后运行 `scripts/Repair-V5Identities.ps1`，确认身份后再加 `-Apply`。
5. 教师和学生页面按 Ctrl+F5 刷新。检查登录、提问、审批、额度和附件功能。

不需要重新初始化，不要删除或覆盖 `data`。如需回滚，停止服务后使用补丁前的程序文件和联合备份恢复到新目录；数据库升级后不能只覆盖回旧代码。

## 修复内容

- AI 审核排除完整思考块；多结论、重复字段、截断或非 JSON 结论回退教师审核。
- AI 审核读取待审对话和文本附件全文。含图片或超过 40,000 字符的审核上下文转教师审核，不截断后放行。
- AI 审核使用独立串行执行线程，不阻塞已批准请求的生成；非流式审核请求有总超时和停止取消。
- 教师额度调整遇到明确的服务端拒绝后重新预览；网络结果不确定时继续使用原幂等键。
- 学生页面持续跟踪活动请求直到取得最终状态，修复完成时断网及服务重启后的状态恢复。
- 请求幂等摘要只依赖客户端输入；补丁前的请求按保存的原始快照兼容重试。
- 教师可上传并使用课堂附件，不需要进入学生名册，也不产生学生额度；已提交的教师附件保留归档。
- 访客链接使用可访问的教师机地址，提供局域网候选地址并允许修改，阻止复制 localhost / 127.0.0.1 分享地址。
- 后续加固：审核任务异常与生成隔离，连续失败暂停 AI 审核；非流式审核使用独立超时和 90 秒重试总预算；图片/超长/超时分项转人工；v5 升级预检；网卡枚举分享地址；教师附件按引用保留并可清理无引用文件。

## 校验和构建

压缩包旁的 `.sha256` 可用于核对下载完整性；包内 `patch-manifest-2.1.8-audit1.json` 记录每个文件的 SHA-256。

源码构建：运行 `scripts/Build-Patch.ps1`，或使用 Python 执行 `app/build_patch.py --output <新补丁.zip>`。构建器拒绝覆盖已有补丁。

已通过 110 项 Python 测试、4 个前端交互探针，以及 Open WebUI 0.11.2 原生登录、改密、文件上传、学生审批与教师附件对话链路。回归验证使用隔离账号、临时数据库和模拟提供商，不调用真实付费模型。真实提供商与校园网部署仍按教师指南验收。
